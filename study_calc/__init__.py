"""Study Calculator — physics, symbolic math, unit conversion and learning materials.

The package has three layers:

* :mod:`study_calc.core`    — the formula model with a solver for any variable,
  and the unit converter;
* :mod:`study_calc.domains` — ready-made formula sets grouped by physics section;
* :mod:`study_calc.gui`     — the Tkinter desktop interface.

User-facing text is localized via :mod:`study_calc.i18n` (English, Spanish,
French, Russian, Ukrainian), switchable at runtime.
"""

__version__ = "0.4.0"
