"""The redesign frontend layer (ADR 0001).

Currently holds the framework-agnostic design tokens (``tokens.json`` +
``tokens.py``). The PyWebView host window and the JS<->Python bridge land with
issue #4.
"""
