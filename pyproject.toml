[project]
name = "physics-calc"
version = "0.2.0"
description = "Physics calculator: mechanics, thermodynamics, electromagnetism, waves and a unit converter with a Tkinter GUI (English/French/Russian)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Danil Chernyshev", email = "danil.chernyshev@gmail.com" }]
keywords = ["physics", "calculator", "units", "tkinter", "gui"]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
physics-calc = "physics_calc.gui:run"

[project.gui-scripts]
physics-calc-gui = "physics_calc.gui:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["physics_calc"]

[tool.pytest.ini_options]
testpaths = ["tests"]
