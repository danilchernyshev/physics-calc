# <ticket> — study plan  ·  owner: curriculum-author

## Plan
| Subject | Course (code) | Topic id | Intended problems | Notes |
|---------|---------------|----------|-------------------|-------|
| | | | | |

## Engine dependencies (route to dev-manager)
- [ ] new formula/unit/error code needed first? → `python-pro` (phase 04), or "none"
- [ ] new content field/table/kind (schema change)? → `sql-pro` (phase 04, `schema.sql` + `seed_db.py`), or "none"
- [ ] otherwise content-only: author JSON, then reseed (`python scripts/seed_db.py`)

## Locale plan
- en authored as canonical; es/fr/uk additive later.
